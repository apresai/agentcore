# AgentCore Quickstart Example
# https://docs.aws.amazon.com/bedrock-agentcore/

# Core AgentCore SDK
bedrock-agentcore>=1.18.1

# CLI for agentcore dev / deploy / invoke
bedrock-agentcore-starter-toolkit>=0.3.10

# Agent framework (Strands is the recommended starting point)
strands-agents>=1.50.0
