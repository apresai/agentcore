# AgentCore Runtime Example
# https://docs.aws.amazon.com/bedrock-agentcore/

# Core AgentCore SDK
bedrock-agentcore>=1.18.1

# CLI for agentcore configure / deploy / invoke
bedrock-agentcore-starter-toolkit>=0.3.10

# Agent framework (Strands is the simplest to start with)
strands-agents>=1.50.0

# AWS SDK for lower-level access
boto3>=1.43.0
